import * as styledComponents from "styled-components";

const {
  default: styled,
  createGlobalStyle,
  css,
  keyframes,
} = styledComponents;

export { createGlobalStyle, css, keyframes, styled };
